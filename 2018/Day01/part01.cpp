#include <bits/stdc++.h>
#define debug(x) cout << #x " = " << (x) << endl

using namespace std;

int main(){

    int input, sum = 0;

    while(cin >> input){
        sum += input;
    }

    debug(sum);

    return 0;
}
